const f=document.querySelector('#contactForm');if(f){f.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(f);const subject=encodeURIComponent(`Anfrage TEILHABE.LAB: ${data.get('thema')||'Fortbildung/Beratung'}`);const body=encodeURIComponent(`Name: ${data.get('name')}\nOrganisation: ${data.get('org')}\nE-Mail: ${data.get('email')}\nThema: ${data.get('thema')}\n\nNachricht:\n${data.get('message')}`);const target=f.dataset.email||'';if(!target){const out=document.querySelector('#contactHint');out.textContent='Die geschäftliche Kontaktadresse ist noch nicht hinterlegt. Bitte vor der Veröffentlichung in kontakt.html bei data-email ergänzen.';out.classList.remove('hidden');return}location.href=`mailto:${target}?subject=${subject}&body=${body}`;});}
